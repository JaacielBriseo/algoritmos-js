/**
 * Crea un programa que tome un string como parametro
 * y cuente las vocales que hay en todo el string.
 * Ejemplo:
 * countVowels('hello') --> 2
 * countVowels('aeiou') --> 5
 * countVowels('hhhhh') --> 0
 */
function countVowels(str) {
	
}

module.exports = countVowels;
