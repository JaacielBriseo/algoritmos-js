/**
 * Escribe una función que tome un objeto como parámetro
 * y devuelva un array con los nombres (keys) de todas las propiedades del objeto.
 */

const getObjectPropertyNames = (obj) => {
  
};

module.exports = getObjectPropertyNames